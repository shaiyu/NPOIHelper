using System;
using System.Collections.Generic;
using System.Text;

namespace NPOIHelper
{
    public class TitleNameAttribute : ValueAttribute
    {
        public TitleNameAttribute(string value) : base(value)
        {
        }
    }
}
